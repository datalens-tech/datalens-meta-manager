import {EntryScope} from './types/entry';

export const ENTRY_SCOPES = Object.values(EntryScope);

export const US_ERRORS = {
    VALIDATION_ERROR: 'VALIDATION_ERROR',
    WORKBOOK_ALREADY_EXISTS: 'WORKBOOK_ALREADY_EXISTS',
};
